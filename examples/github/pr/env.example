# Set your GitHub token (classic PAT or fine-grained with repo scope).
# Create at: GitHub → Settings → Developer settings → Personal access tokens
# For 'gh' CLI: scope repo (or full control of private repositories).
GITHUB_TOKEN=ghp_your_token_here
