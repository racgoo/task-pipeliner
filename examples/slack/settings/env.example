# Copy to env and set a Slack Bot User OAuth Token (xoxb-...).
# Create an app at api.slack.com/apps → OAuth & Permissions → Bot User OAuth Token.
# Scopes: team:read (for team.info) or none for auth.test only.
SLACK_TOKEN=xoxb-your-bot-token
